/**
 * Configurações de exibição compartilhadas — centralizadas pra evitar
 * hardcode em múltiplos componentes.
 */

/** Quantos participantes aparecem no gráfico multi-linha (top N por pontos). */
export const TOP_N_GRAFICO = 15;
